import { findTelemetryData } from "./FindTelemetryData";

export function dataToGraphDataHeel(dataGroup, absoluteTime) {
    // takes the info from now until closest value in FindTelemetyData converts it into {timestamp: , heel1: , heel2 etc.} write to heel
    dataGroup.map(group => {
        
    });
}